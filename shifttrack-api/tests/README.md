# Test Workspace

This folder groups all backend test projects by suite type:

- `Unit`
- `Integration`
- `Acceptance`
- `Shared`

Use `Shared` for cross-suite artifacts that should not live in a single project:

- mocks
- fixtures
- builders
- reusable support helpers
